import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Post Analyzer - Advanced C++ Text Analysis",
  description: "A Next.js application for analyzing posts with C++ backend processing, database storage, and ML integration.",
  keywords: ["post analyzer", "text analysis", "C++", "Next.js", "machine learning"],
  authors: [{ name: "Post Analyzer Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
